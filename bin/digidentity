#!/bin/bash

tmux has-session -t digidentity 

if [ $? != 0 ]
then
  tmux new-session -s digidentity -n vagrant -d
  tmux send-keys -t digidentity 'cd ~/Projects/dmtw/' C-m
fi

tmux attach -t digidentity

